// run `node index.js` in the terminal

// don't remove this line if are using prompt
const prompt = require('prompt-sync')();

const firstName = 'Ali';
const lastName = 'Nor Azmy';
const fullName = `${firstName} ${lastName}`;

console.log(fullName);
